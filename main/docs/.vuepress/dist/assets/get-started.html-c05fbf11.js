import{_ as e,o as a,c as t,a as s}from"./app-d76978d4.js";const c={},r=s(`<h1 id="get-started" tabindex="-1"><a class="header-anchor" href="#get-started" aria-hidden="true">#</a> Get started</h1><h2 id="repl" tabindex="-1"><a class="header-anchor" href="#repl" aria-hidden="true">#</a> REPL</h2><p>Run <code>buzz</code> without arguments to start the REPL:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>buzz
</code></pre></div><p>The REPL is useful for quick experiments. It imports <code>buzz:std</code> and <code>buzz:debug</code> automatically, keeps successful declarations between submissions, and prints the value of expressions:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt;&gt;&gt; 1 + 2
3
&gt;&gt;&gt; final name = &quot;buzz&quot;;
&gt;&gt;&gt; std\\print(&quot;hello {name}&quot;);
hello buzz
</code></pre></div><p>If you are building buzz from source, use <code>zig build run</code> to start the same REPL from the build tree.</p><h2 id="standalone-scripts" tabindex="-1"><a class="header-anchor" href="#standalone-scripts" aria-hidden="true">#</a> Standalone scripts</h2><p>Use <code>buzz run-script</code> to run a single script file:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>buzz run-script <span class="token operator">&lt;</span>script<span class="token operator">&gt;</span>.buzz
</code></pre></div><p>Any arguments after the script path are passed to the script&#39;s <code>main</code> function:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>buzz run-script hello.buzz Alice
</code></pre></div><h2 id="package" tabindex="-1"><a class="header-anchor" href="#package" aria-hidden="true">#</a> Package</h2><p>Use <code>buzz init</code> to create a minimal package in the current directory:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>buzz init
</code></pre></div><p>The command asks for package metadata, then writes <code>manifest.buzz</code>, <code>build.zig</code>, <code>src/main.buzz</code>, a small library example, and a <code>vendors/&lt;package-name&gt;</code> link back to the package. After that, run:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>buzz run
</code></pre></div><p><code>buzz run</code> executes <code>src/main.buzz</code> from the current package and passes any extra command-line arguments to that script.</p>`,18),d=[r];function n(o,i){return a(),t("div",null,d)}const l=e(c,[["render",n],["__file","get-started.html.vue"]]);export{l as default};
